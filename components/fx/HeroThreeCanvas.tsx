'use client';

/**
 * Optional WebGL layer for the hero. Placeholder keeps the static export build green;
 * replace with a three.js scene when you add visuals.
 */
export function HeroThreeCanvas() {
  return null;
}
